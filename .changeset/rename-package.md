---
'@postnzt/docx-editor': minor
---

Rename the published package from `@postnzt/docx-js-editor` to `@postnzt/docx-editor`. Update imports and run `npm install @postnzt/docx-editor`. The previous package name (`@postnzt/docx-js-editor`) is deprecated and will not receive further updates.
